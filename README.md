
# MAGLS_Protection 👑🔥

بوت حماية متكامل لسيرفرات ديسكورد، خاص بـ **MAGLS ALSHAMSI**.

## ⭐ المميزات

- حماية من:
  - حذف القنوات (مع استرجاعها تلقائياً)
  - تعديل القنوات (الاسم / الكاتيجوري)
  - تغيير اسم السيرفر
  - إنشاء رتب جديدة بدون إذن
  - تعديل رتب الأعضاء (إضافة / إزالة)
  - إعطاء باند بدون إذن (يتم فك الباند)
  - إضافة بوتات بدون إذن

- نظام **Whitelist** للأشخاص فوق الحماية
- أمر سحري:

```text
run @الشخص
```

لإضافة شخص إلى قائمة الحماية.

## 📁 محتويات المشروع

- `index.js` → كود البوت الكامل
- `package.json` → إعدادات المشروع
- `.env` (أنت تنشئه) أو تستخدم `.env.example`
- `README.md` → هذا الملف

## 🚀 طريقة التشغيل (Replit أو أي استضافة Node.js)

1. ثبت الحزم:

```bash
npm install
```

2. أنشئ ملف `.env` بجانب `index.js` وضع داخله:

```env
TOKEN=هنا_توكن_البوت_الخاص_بك
```

3. شغل البوت:

```bash
npm start
```

أو:

```bash
node index.js
```

## ⚠️ تنبيه مهم

> لا ترفع ملف `.env` إلى GitHub أبداً  
> ولا تكتب التوكن داخل `index.js`.

## 👑 أمر الحماية

لإضافة شخص فوق الحماية:

```text
run @الشخص
```

هو فقط المالك (صاحب ID: 1253251616765775882) الذي يستطيع استخدام هذا الأمر.
